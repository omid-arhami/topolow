% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/euclidify_diagnostics.R
\name{plot_euclidify_diagnostics}
\alias{plot_euclidify_diagnostics}
\title{Plot Euclidify Optimization Diagnostics}
\usage{
plot_euclidify_diagnostics(
  euclidify_result,
  plot_types = "all",
  save_plots = TRUE,
  output_dir = NULL,
  width = 12,
  height = 8,
  dpi = 300,
  return_plots = TRUE
)
}
\arguments{
\item{euclidify_result}{A result object from \code{Euclidify()} with create_diagnostic_plots=TRUE.}

\item{plot_types}{Character vector specifying which plots to create. Options:
"all", "parameter_search", "convergence", "quality", "cv_errors".
Default: "all"}

\item{save_plots}{Logical. Whether to save plots to files. Default: TRUE}

\item{output_dir}{Character. Directory for saving plots. Required if save_plots=TRUE.}

\item{width}{Numeric. Plot width in inches. Default: 12}

\item{height}{Numeric. Plot height in inches. Default: 8}

\item{dpi}{Numeric. Resolution for saved plots. Default: 300}

\item{return_plots}{Logical. Whether to return plot objects. Default: TRUE}
}
\value{
A list of ggplot objects if return_plots=TRUE, otherwise NULL invisibly.
}
\description{
Creates comprehensive diagnostic plots for the Euclidify optimization process,
including parameter search trajectory and embedding quality metrics.
}
\details{
This function creates several diagnostic visualizations:
\itemize{
\item Parameter Search: Shows explored parameter space in 2D projections
\item Convergence Trace: Plots MAE and parameter evolution during final embedding
\item Quality Metrics: Scatter plots of predicted vs true distances
\item CV Errors: Distribution of cross-validation errors across folds
}
}
\examples{
\dontrun{
# Run Euclidify with diagnostic plots
result <- Euclidify(
  dissimilarity_matrix = my_data,
  output_dir = "output",
  create_diagnostic_plots = TRUE
)

# Plots are automatically saved to output/diagnostics/
# View diagnostic report
report <- create_diagnostic_report(result)
cat(report, sep = "\n")

# Access specific diagnostic plots
print(result$diagnostic_plots$parameter_search)
print(result$diagnostic_plots$quality)
}

}
